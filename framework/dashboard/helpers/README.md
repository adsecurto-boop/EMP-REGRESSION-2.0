# framework/dashboard/helpers

Folder scaffold only (Phase 5).

Only what proves shared in practice: safe-read wrappers, displayed-timestamp capture (raw string + flagged parse -- timezone semantics unresolved), artifact capture via ArtifactManager. Anything duplicating framework/shared/utils (retry, datetime tolerance, hashing) is a review-blocking defect.
