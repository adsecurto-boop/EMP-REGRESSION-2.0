# framework/dashboard/locators

Folder scaffold only (Phase 5).

Central locator registries, one module per page/component -- **empty until observation**. Every locator carries provenance (observed date, dashboard version, source recording, tier) per the [Locator Standard](../../../docs/ADS/locator_standard.md). An inline selector anywhere else in this package is a review-blocking defect.
